interface:
  display_name: "SkillFoundry"
  short_description: "Turns real demand signals into reviewed, publishable skills."
  default_prompt: "Use $skill-foundry to discover recent user needs and popular ClawHub skill opportunities, generate reviewed skills in parallel, optionally publish them, and write a catalog."

policy:
  allow_implicit_invocation: true
